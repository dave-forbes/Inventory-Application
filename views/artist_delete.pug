extends layout 

block styles  
  link(rel='stylesheet', href='/stylesheets/delete.css')

block content 
  div.centered 
    p Are you sure you want to delete #{artist.name}?
  
    form(method="post")
      input(type='hidden', name='artist', value=artist._id ) 
      button.btn.light-red(type='submit') Delete?